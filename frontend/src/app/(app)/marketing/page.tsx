import { ComingSoon } from '@/components/ui/coming-soon';

export default function MarketingPage() {
  return (
    <ComingSoon
      title="Маркетинг"
      description="Учёт рекламных кампаний по каналам (Instagram, TikTok, Google Ads и др.), ROI = ((Доход − Расход) / Расход) × 100, и Unit-экономика: CPL, CAC, LTV, ROMI. Кампании уже хранятся в модели MarketingCampaign."
    />
  );
}
